{% extends "admin/auth.html.twig" %}

{% block message %}
<div class="login-message">
    {% if message.text is defined %}<div class="alert alert-{{ message.type }}">{{ message.text }}</div>{% endif %}
</div>
{% endblock %}

{% block content %}
<div class="login-content">
    <div class="login-header">Восстановление пароля</div>
    <div class="login-form">
        <form method="post">
            <div class="form-group">
                <label for="inputLogin" class="control-label">Логин или эл. почта</label>
                <input type="text" class="form-control" id="inputLogin" name="_user">
            </div>
            <button class="btn btn-default btn-lg">Запросить пароль</button>
        </form>
    </div>
    <div class="login-footer">
        Если вы забыли пароль, введите ваш Логин или адрес Электронной почты, указанный при регистрации.<br>
        Новый пароль будет выслан вам по электронной почте.<br>
        Вернуться на <a href="{{ path('admin_index') }}/">форму авторизации</a>.
    </div>
</div>
{% endblock %}